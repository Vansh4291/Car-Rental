A full-stack(MERN) car rental application with a React frontend and a Node.js backend(Express.js).
The React client lets users browse cars and create bookings, while the backend handles APIs for cars, users, and rental operations.
